# Curated Annotation Notes

This folder is reserved for cross-topic annotation guidance for v0.3 evals.

The current fixtures store their gold annotations directly inside each topic directory to keep tests simple and offline.
