# GapForge v0.4.1 Codex Usage Status

Status: **real Codex/GPT-5.4 workflow acceptance passed locally**

Date: May 6, 2026

## Environment

The local verification run used:

- `GAPFORGE_ENABLE_REAL_RUNS=1`
- `GAPFORGE_AGENT_MODE=direct`
- `GAPFORGE_AGENT_NAME=codex`
- `GAPFORGE_CODEX_MODEL=gpt-5.4`
- installed `codex` CLI through `scripts/gapforge_codex_exec_task.sh`

## What Was Validated

- Codex setup detection reports direct mode as available when the local CLI is configured.
- Codex task packs can be executed through direct `codex exec`.
- Codex outputs are written to the expected task `outputs/` directory.
- `validate-import-all` discovers and imports valid outputs.
- Empty conservative patch files can be accepted as safe no-op outputs when the task contract allows partial output.
- Real-run attestation records the agent, model, method, validation/import status, and remaining blockers.
- Campaign human review records acceptance and blocks fake citations, unsupported high-confidence claims, and overclaimed novelty.
- `gapforge v4-release-gate` passes only after the real campaign threshold and required campaign mix are satisfied.

## Canaries Run

| Canary | Campaign ID | Task Type | Output | Acceptance |
| --- | --- | --- | --- | --- |
| `single_task_codex_handoff` | `campaign-20260506T001345Z-single-task-codex-handoff-validation` | `novelty_reviewer` | conservative no-op novelty patches | accepted as refusal/unknown novelty workflow |
| `manual_pdf_codex_reading_handoff` | `campaign-20260506T002446Z-manual-pdf-or-fixture-section-codex-reading-validation` | `deep_reader_batch` | conservative no-op reading patches | accepted as manual-PDF/full-text workflow |
| `single_task_codex_handoff` | `campaign-20260506T002805Z-single-task-codex-handoff-validation` | `novelty_reviewer` | unknown novelty dossier patch plus no-op supporting files | accepted as fixture-backed experiment-ready workflow |

## Release Gate Status

Command:

```bash
gapforge v4-release-gate --write-report --json
```

Observed status:

- passed: true
- accepted real campaigns: 3
- experiment-ready campaign present: true
- refusal campaign present: true
- manual-PDF/full-text campaign present: true
- blockers: none

## Important Limits

These canaries validate the Codex workflow mechanics. They do not establish that GapForge can perform exhaustive autonomous literature review, and they do not validate broad research quality across live scientific domains.

Fake-agent canaries remain useful for CI but never count as real Codex/GPT-5.4 acceptance. Task-pack/manual-handoff output counts only after validation/import, attestation, and human review.
