# GapForge v0.4.0 Real-Run Acceptance

Status: **v0.4 actual-run acceptance not completed**

This document records the May 5, 2026 v0.4 validation pass. It separates deterministic checks, fake-agent campaign validation, and actual Codex/GPT-5.4 campaign acceptance.

## Deterministic Validation

Status: **passed**

Commands run:

- `make format`
- `make format-check`
- `make lint`
- `make typecheck`
- `make test`
- `make eval`
- `gapforge eval --v2 --write-report`
- `gapforge eval --v3 --write-report`
- `gapforge eval --v4 --write-report`
- `make coverage`
- `make v2-smoke`
- `make v3-smoke`
- `make v4-smoke`

Observed results:

- `make test`: 417 passed.
- `make coverage`: 417 passed, coverage report generated.
- v2 eval overall score: 0.872.
- v3 eval overall score: 0.732.
- v4 eval overall score: 0.705.
- v2, v3, and v4 smoke targets completed. The v4 smoke target writes a failed release-gate report because fake-agent validation cannot satisfy real-run acceptance.

## Fake-Agent Campaign Validation

Status: **passed, but not actual-run acceptance**

Profile:

- `fake_agent_campaign_regression`

Observed:

- The fake campaign canary completed.
- The campaign record labeled `actual_run_status` as `fake_not_actual`.
- The v4 release gate correctly rejected fake-agent campaigns as actual Codex/GPT-5.4 evidence.

Fake-agent success verifies schema, campaign, task-pack, and validation plumbing only. It never counts as real Codex/GPT-5.4 acceptance.

## Actual Codex/GPT-5.4 Campaign Validation

Status: **not completed**

Required environment was not present:

- `GAPFORGE_ENABLE_REAL_RUNS=1` was unset.
- `GAPFORGE_AGENT_NAME=codex` was unset.
- `GAPFORGE_CODEX_MODEL=gpt-5.4` was unset.
- No direct `GAPFORGE_CODEX_COMMAND` was configured.

Canary outcomes:

| Profile | Topic | Result |
| --- | --- | --- |
| `agentic_low_fpr_collusion` | low false positive collusion detection in LLM agents | Not run; refused because `GAPFORGE_ENABLE_REAL_RUNS=1` was missing. |
| `agentic_monitor_evasion` | lexical substitution attacks on LLM monitors | Not run; refused because `GAPFORGE_ENABLE_REAL_RUNS=1` was missing. |
| `agentic_cross_domain_specificity` | medical screening specificity methods for AI safety false-positive control | Not run; refused because `GAPFORGE_ENABLE_REAL_RUNS=1` was missing. |
| `agentic_undercovered_refusal` | intentionally sparse undercovered research topic | Completed as deterministic/refusal behavior; not an actual Codex/GPT-5.4 campaign. |
| `manual_pdf_agentic` | manual local PDF workflow with Codex-backed reading | Not run; refused because `GAPFORGE_ENABLE_REAL_RUNS=1` was missing. |

No real Codex/GPT-5.4 output was imported. No actual-run attestation was recorded. No campaign human review accepted a real Codex/GPT-5.4 campaign.

## Release Gate Result

Command:

```bash
gapforge v4-release-gate --write-report
```

Result: **failed, as expected**

The generated report from `--write-report` is written under `data/release_gate/` and is intentionally ignored. This file records the stable human-readable release acceptance status.

Release-gate summary:

- Deterministic CI evidence: passed.
- Fake-agent campaign canary: passed.
- Accepted real Codex/GPT-5.4 campaigns: 0.
- Experiment-ready accepted real campaign: missing.
- Strict-refusal accepted real campaign: missing.
- Manual-PDF/full-text accepted real campaign: missing.

Blocking failures:

- Fewer than 3 accepted real Codex/GPT-5.4 campaigns.
- No accepted real campaign produced an experiment-ready or manuscript-ready direction.
- No accepted real campaign correctly refused a recommendation due to poor coverage or novelty uncertainty.
- No accepted real campaign covered a manual-PDF or full-text workflow.
- Fake-agent campaigns cannot count as actual Codex/GPT-5.4 acceptance.

## Release Statement

**v0.4 actual-run acceptance not completed.**

The repository can claim deterministic validation and fake-agent campaign validation for this pass. It cannot claim actual Codex/GPT-5.4 campaign acceptance until real campaigns are executed through direct or task-pack/manual-handoff mode, outputs are validated/imported, Codex/GPT-5.4 execution is attested where needed, campaign human review accepts the outputs, and `gapforge v4-release-gate` passes.
