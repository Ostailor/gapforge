# GapForge v0.4.0 Real-Run Acceptance

Status: **v0.4 actual-run acceptance passed**

This document records the May 6, 2026 v0.4 validation pass. It separates deterministic checks, fake-agent campaign validation, and actual Codex/GPT-5.4 campaign acceptance.

## Deterministic Validation

Status: **passed**

Commands run during the local verification pass included:

- `make format`
- `make format-check`
- `make lint`
- `make typecheck`
- `make test`
- `make eval`
- `gapforge eval --v2 --write-report`
- `gapforge eval --v3 --write-report`
- `gapforge eval --v4 --write-report`
- `make coverage`
- `make v2-smoke`
- `make v3-smoke`
- `make v4-smoke`

Most recent focused verification after the release-gate fix:

- `make format-check`: passed
- `make lint`: passed
- `make typecheck`: passed
- `tests/test_release_gate.py`: 11 passed
- `make test`: 465 passed

## Fake-Agent Campaign Validation

Status: **passed, but not actual-run acceptance**

Profile:

- `fake_agent_campaign_regression`

Fake-agent success verifies schema, campaign, task-pack, validation, and import plumbing only. It never counts as real Codex/GPT-5.4 acceptance.

## Actual Codex/GPT-5.4 Campaign Validation

Status: **passed**

Environment used:

- `GAPFORGE_ENABLE_REAL_RUNS=1`
- `GAPFORGE_AGENT_MODE=direct`
- `GAPFORGE_AGENT_NAME=codex`
- `GAPFORGE_CODEX_MODEL=gpt-5.4`
- direct command: local `scripts/gapforge_codex_exec_task.sh` wrapper around the installed `codex exec` CLI

Accepted real campaigns:

| Profile | Campaign ID | Purpose | Result |
| --- | --- | --- | --- |
| `single_task_codex_handoff` | `campaign-20260506T001345Z-single-task-codex-handoff-validation` | Conservative novelty/refusal workflow | Accepted |
| `manual_pdf_codex_reading_handoff` | `campaign-20260506T002446Z-manual-pdf-or-fixture-section-codex-reading-validation` | Manual-PDF/full-text reading workflow | Accepted |
| `single_task_codex_handoff` | `campaign-20260506T002805Z-single-task-codex-handoff-validation` | Fixture-backed experiment-ready workflow | Accepted |

Each accepted campaign had:

- real Codex/GPT-5.4 output written through direct execution
- validated/imported output patches
- actual-run attestation
- human campaign review acceptance
- source coverage
- retrieval index
- campaign report
- explicit stop reason

The accepted outputs were conservative. The manual-PDF/full-text canary produced no unsupported new claims. The novelty canaries either marked novelty unknown/refusal or used a fixture-backed experiment-ready direction for release-gate workflow validation. These canaries validate the actual-run pathway; they do not claim exhaustive literature-review quality.

## Release Gate Result

Command:

```bash
gapforge v4-release-gate --write-report --json
```

Result: **passed**

Release-gate summary:

- deterministic CI evidence: passed
- fake-agent campaign canary: passed
- accepted real Codex/GPT-5.4 campaigns: 3
- experiment-ready accepted real campaign: present
- strict-refusal accepted real campaign: present
- manual-PDF/full-text accepted real campaign: present
- blockers: none

The generated machine-readable gate report is written under `data/release_gate/` and is intentionally ignored by git. This file records the stable human-readable release acceptance status.

## Blocking Failures

None for the final May 6, 2026 release-gate run.

Historical blockers from the May 5 pass were:

- real-run environment not configured
- no accepted real Codex/GPT-5.4 campaigns
- no validated/imported real Codex outputs
- no actual-run attestation
- no human-reviewed actual-run campaign acceptance

Those blockers were resolved for the May 6 workflow canary validation.

## Release Statement

**v0.4 actual-run acceptance passed.**

This statement is limited to the v0.4 release-gate workflow canaries above. GapForge remains a conservative research ideation system, not an exhaustive autonomous literature reviewer.
