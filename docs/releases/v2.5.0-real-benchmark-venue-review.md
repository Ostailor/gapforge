# GapForge v2.5.0 Real Benchmark, Venue, and Review Record

This record preserves the evidence boundary for v2.5.0. The release adds real benchmark grounding machinery, venue-style manuscript shaping, and drastic OpenReview-calibrated critique. The selected manuscript remains a revision target, not a top-conference-ready submission.

## Benchmark Fit Boundary

The live v2.5 verification path registers `Synthetic sequential low-FPR monitoring fixture` as a vetted benchmark record because it is an allowed local fixture with explicit license/terms, no download requirement, no authentication requirement, and transparent provenance.

Fit assessment and mapping are explicit:

- The fixture supports sequential low-FPR monitoring plumbing and adapter tests.
- The fixture does not prove real-world collusion detection validity.
- The fixture does not remove the need for the selected benchmark protocol.
- Unsupported claims include deployment validity and real collusion-trace validity.

The adapter preserves original labels and splits and records a warning that adapted examples must not be described as real collusion traces.

## Venue Style Boundary

The v2.5 venue-style path uses a synthetic TeX fixture to extract structural features only:

- section order
- abstract length
- related-work placement
- experiment section placement
- limitations placement
- figure/table density
- caveat and contribution-framing patterns

No source prose is copied into the manuscript. The style corpus is too small for strong inference, so recommendations are weak structural/rhetorical guidance only.

The venue rewrite creates a `generic_ml_conference`-shaped manuscript package, but the package is `not_publication_ready` because bibliography and anonymization blockers remain open.

## Reviewer Calibration Boundary

The OpenReview-style dataset is a synthetic fixture:

- 2 papers
- 3 reviews
- hashed reviewer identifiers
- heuristic labels
- auditable issue taxonomy

Reviewer training and evaluation calibrate critique behavior. They do not claim human reviewer equivalence and cannot invent citations, results, datasets, or artifact claims.

The reviewer evaluation reports:

- issue recall proxy: 0.5714
- severity calibration score: 0.9167
- review specificity score: 0.2857
- hallucination/fake-citation rate: 0.0
- evidence linkage score: 1.0

## Drastic Review Boundary

The drastic review panel is preserved as a blocker, not rewritten away:

- likely decision: `reject_likely`
- workshop candidate: true after limitations/evidence labels are sharpened
- fatal flaws: 2
- required revisions: 5

Fatal blockers:

- missing auditable related-work matrix for novelty calibration
- missing review-ready artifact evaluation package

The revision plan creates concrete tasks for fatal fixes, major fixes, new experiments, related-work requests, claim softening, manuscript rewrites, and artifact updates. A rebuttal cannot excuse these missing-evidence blockers.

## Release Gate Interpretation

The v2.5 release gate confirms that:

- v2.4 still passes
- vetted benchmark registry exists
- selected idea has a mapping report
- adapter and vetted experiment artifacts exist
- venue profile, style corpus, style analysis, and venue rewrite exist
- OpenReview-style fixture, taxonomy labels, reviewer evaluation, and calibration report exist
- drastic review and drastic revision plan exist
- fake citations/results are blocked
- copied paper prose is blocked
- no publication-ready claim is allowed while drastic fatal blockers remain

The resulting v2.5 gate status is `revise_for_reviews`. This is the intended honest outcome for the current manuscript state.
