# GapForge v2.0.1 Release Notes

Release date: 2026-05-08

GapForge v2.0.1 is a CI fixture tracking patch. It records the narrow post-v2.0 fix that keeps historical migration fixture JSONs available in clean GitHub checkouts after broad generated-artifact ignore rules hid them from the release commit.

## Scope

- No product behavior changed.
- No idea-discovery claims changed.
- The v2.0 selected idea remains the same: `idea-sequential-specificity-benchmark-for-low-fpr-collusion-audits`.
- No unsafe generated artifacts were committed.
- The tracked migration fixture JSONs are small, synthetic compatibility fixtures only.

## Fixture Safety

- Historical migration fixture JSON payloads under `tests/fixtures/migrations/` are committed as deterministic test inputs.
- The largest migration fixture JSON is 1,336 bytes; all migration fixture JSONs total 11,934 bytes.
- Generated `.omx` state, release-gate report mirrors, `runs/`, `projects/`, caches, PDFs, transcripts, dashboards, and downloaded paper artifacts remain ignored.

## Tests Run

- `make ci`
- `gapforge compatibility-audit --v2 --fixtures --write-report`
- `gapforge v2-release-gate --write-report --json`
- GitHub Actions CI on clean checkout for commit `f2c543568e66e1d9a8bbc369c3825a81cd55a869`

## Remaining Risks

- v2.0.1 does not add new release evidence beyond the clean-checkout CI fixture tracking fix.
- The v2.0 selected idea remains candidate-level only and still needs real benchmark/data construction, experiment execution, manuscript work, and external scientific review.
